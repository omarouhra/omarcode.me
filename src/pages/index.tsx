import Hero from "@/components/Hero";
import LogoIcon from "@/icons/LogoIcon";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default Home;
